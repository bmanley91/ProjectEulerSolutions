apply plugin: 'findbugs'

findbugs {
    ignoreFailures = false
    effort = "max"
    reportLevel = "low"
}

tasks.withType(FindBugs) {
    reports {
        xml.enabled = false
        html.enabled = true
    }
}
