group 'bmanley'
version '1.0-SNAPSHOT'

apply from: file("$rootDir/gradle/pmd.gradle")
apply from: file("$rootDir/gradle/findBugs.gradle")

apply plugin: 'java'

repositories {
    mavenCentral()
}

dependencies {
    testCompile group: 'junit', name: 'junit', version: '4.11'
}
